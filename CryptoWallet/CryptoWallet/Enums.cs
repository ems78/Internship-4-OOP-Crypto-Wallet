namespace CryptoWallet
{
    public enum WalletType
    {
        bitcoin,
        ethereum,
        solana
    }

    public enum TransactionType
    {
        fungible,
        nonFungible
    }
}
